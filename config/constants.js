export const ROLES = Object.freeze({
  STUDENT: "Alumno",
  ADMIN: "Administrador"
});

export const TABLES = Object.freeze({
  PROFILES: "profiles",
  SEMESTERS: "semestres",
  SUBJECTS: "asignaturas",
  EVALUATIONS: "evaluaciones"
});

export const THEME_KEY = "notasUniversitariasTheme";

export const DEFAULTS = Object.freeze({
  presentationPercent: 60,
  examPercent: 40,
  approvalGrade: 4,
  exemptionGrade: 5.5,
  simulationExamGrade: 4
});
